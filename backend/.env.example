# MongoDB Connection String (Atlas URI or Local MongoDB)
# Leave blank to use zero-setup local persistent fallback
MONGODB_URI=mongodb://localhost:27017
DB_NAME=pulseops_db

# Port & Server settings
PORT=8000
HOST=127.0.0.1
